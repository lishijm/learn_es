#include"../include/link.h"

int main(){
    
    clink();
    /*int i;
    for(i=0;i<5;i++){
        plist->arr[i]=i;
    }

    int sel,pos,flag=1;
    listel posel;
    while(flag){
        scanf("%d",&sel);
        switch(sel){
            case 1:
                output(plist);
                break;
            case 2:
                scanf("%d",&pos);
                scanf("%d",&posel);
                insert(plist,pos,posel);
                break;
            case 3:
                scanf("%d",&pos);
                scanf("%d",&posel);
                del(plist,pos);
            default:
                flag=0;
        }
    }*/

    return 0;
}